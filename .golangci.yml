version: "2"

linters:
  enable:
    - govet
    - ineffassign
    - misspell
    - revive
    - staticcheck
    - unconvert
    - unused
  exclusions:
    rules:
      - path: web/node_modules/
        linters: [revive]

formatters:
  enable:
    - gofmt
