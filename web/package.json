{
  "name": "storhub-console",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "nuxt typecheck",
    "test": "vitest run",
    "build:embed": "nuxt generate && node scripts/copy-dist.mjs"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "nuxt": "^4.5.2",
    "tailwindcss": "^4.3.3",
    "vue": "^3.5.41"
  },
  "devDependencies": {
    "@nuxt/eslint": "^1.12.1",
    "eslint": "^10.9.1",
    "typescript": "^6",
    "vitest": "^4.1.11",
    "vue-tsc": "^3.2.1"
  },
  "engines": {
    "bun": ">=1.2"
  },
  "trustedDependencies": [
    "unrs-resolver"
  ]
}
